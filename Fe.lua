if workspace:FindFirstChild("FEBYPASSLOAD") then
  warn("How would you already even have it loaded?")
  return
else
end

local Version = 69.420

local settings = {
  idiot = true;
}

Instance.new("Folder", game:GetService("Workspace")).Name = "FEBYPASSLOAD"

game.Players.LocalPlayer:Kick("You're Retarded")